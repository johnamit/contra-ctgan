# ContraCTGAN

Synthetic credit-card transaction generation with CTGAN and contrastive objectives.
- Train models and generate synthetic tabular datasets
- Evaluate fidelity/utility vs. real data

Data is not stored in the repo (see Datasets/README.md). Model weights are tracked via Git LFS.

## Quick start
pip install -r requirements.txt
python Scripts/contrastive_ctgan.py